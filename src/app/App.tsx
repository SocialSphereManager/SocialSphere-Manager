import React, {Text, View} from 'react-native';
import NavigationManager from '../navigation/navigationManager';

const App = () => {
  return (
    <View>
      <NavigationManager />
    </View>
  );
};

export default App;
